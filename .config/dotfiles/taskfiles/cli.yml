version: "3"

vars:
  FONT_DIR: /usr/share/fonts/truetype/MesloLGS

  # Global npm packages. One per line, `#` comments allowed.
  # Installed together by `task cli:npm-globals`.
  NPM_PKGS: |
    js-beautify
    live-server
    livedown
    neovim
    npm-check-updates
    sass
    sql-lint
    tldr

  # pipx-installed apps. One per line, `#` comments allowed.
  # Installed one at a time by `task cli:pipx-apps` (pipx install takes a single spec).
  PIPX_PKGS: |
    black
    flake8
    isort
    jedi-language-server
    pipreqs
    pycodestyle
    pynvim
    yt-dlp

  # cargo-installed binaries. One per line, `#` comments allowed.
  # Installed one at a time by `task cli:cargo-apps`.
  CARGO_PKGS: |
    alacritty
    neocmakelsp

tasks:
  keygen:
    desc: Generate an SSH key if one doesn't exist yet
    requires:
      vars: [GIT_EMAIL, SSH_KEY_PASSPHRASE]
    status:
      - test -f "$HOME/.ssh/id_ed25519.pub"
    env:
      SSH_KEY_PASSPHRASE: "{{.SSH_KEY_PASSPHRASE}}"
    cmds:
      - |
        [ -n "$SSH_KEY_PASSPHRASE" ] || { echo "E: SSH_KEY_PASSPHRASE is empty" >&2; exit 1; }
        ssh-keygen -t ed25519 -f "$HOME/.ssh/id_ed25519" -C "{{.GIT_EMAIL}}" -N "$SSH_KEY_PASSPHRASE"

  git-identity:
    internal: true
    requires:
      vars: [GIT_NAME, GIT_EMAIL]
    cmds:
      - git config --global user.name "{{.GIT_NAME}}"
      - git config --global user.email "{{.GIT_EMAIL}}"
      - git config --global color.ui true
      - git config --global core.editor vim
      - git config --global gpg.format ssh
      - git config --global user.signingkey "$HOME/.ssh/id_ed25519.pub"
      - git config --global commit.gpgsign true
      - git config --global tag.gpgsign true

  font:
    internal: true
    status:
      - test -f "{{.FONT_DIR}}/MesloLGS-Regular.ttf"
    cmds:
      - |
        tmp=$(mktemp -d)
        trap 'rm -rf "$tmp"' EXIT
        base='https://github.com/romkatv/powerlevel10k-media/raw/master/MesloLGS%20NF'
        curl -fsSL "$base%20Regular.ttf" -o "$tmp/MesloLGS-Regular.ttf"
        curl -fsSL "$base%20Bold.ttf" -o "$tmp/MesloLGS-Bold.ttf"
        curl -fsSL "$base%20Italic.ttf" -o "$tmp/MesloLGS-Italic.ttf"
        curl -fsSL "$base%20Bold%20Italic.ttf" -o "$tmp/MesloLGS-BoldItalic.ttf"
        sudo mkdir -p "{{.FONT_DIR}}"
        sudo cp "$tmp"/*.ttf "{{.FONT_DIR}}/"
        fc-cache -f "{{.FONT_DIR}}"

  submodules:
    internal: true
    cmds:
      - git --git-dir="$HOME/.dotfiles" --work-tree="$HOME" submodule update --init --remote --recursive

  tpm:
    internal: true
    cmds:
      - "$HOME/.tmux/plugins/tpm/bin/install_plugins"

  swift-map:
    internal: true
    status:
      - test -x "$HOME/programs/swift-map/mainloop.py"
    cmds:
      - chmod +x "$HOME/programs/swift-map/mainloop.py"
      - sudo "$HOME/programs/swift-map/mainloop.py" install

  wmutils:
    internal: true
    status:
      - command -v wtf
    cmds:
      - mkdir -p "$HOME/programs"
      - test -d "$HOME/programs/core" || git clone https://github.com/wmutils/core.git "$HOME/programs/core"
      - make -C "$HOME/programs/core"
      - sudo make -C "$HOME/programs/core" install

  fzf:
    internal: true
    status:
      - test -f "$HOME/.fzf.zsh"
    cmds:
      - "$HOME/.fzf/install --all --no-update-rc"

  fzf-update:
    internal: true
    cmds:
      - "$HOME/.fzf/install --all --no-update-rc"

  npm-globals:
    internal: true
    cmds:
      - |
        export NVM_DIR="$HOME/.nvm"
        [[ -s "$NVM_DIR/nvm.sh" ]] && . "$NVM_DIR/nvm.sh"
        command -v npm >/dev/null || { echo "E: npm not found - install node first (task lang:install)" >&2; exit 1; }
        pkgs=$(grep -vE '^\s*#|^\s*$' <<< "{{.NPM_PKGS}}")
        if [[ -n "$pkgs" ]]; then
          echo "$pkgs" | xargs npm i -g --quiet
        fi

  pipx-apps:
    internal: true
    cmds:
      - |
        command -v pipx >/dev/null || { echo "W: pipx not found, skipping" >&2; exit 0; }
        grep -vE '^\s*#|^\s*$' <<< "{{.PIPX_PKGS}}" | xargs -r -n1 pipx install
        pipx upgrade-all

  rustup:
    internal: true
    status:
      - test -x "$HOME/.cargo/bin/rustup"
    cmds:
      - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --no-modify-path

  rustup-update:
    internal: true
    cmds:
      - |
        [[ -s "$HOME/.cargo/env" ]] && . "$HOME/.cargo/env"
        command -v rustup >/dev/null && rustup update
        exit 0

  cargo-apps:
    internal: true
    cmds:
      - |
        [[ -s "$HOME/.cargo/env" ]] && . "$HOME/.cargo/env"
        command -v cargo >/dev/null || { echo "W: cargo not found, skipping" >&2; exit 0; }
        grep -vE '^\s*#|^\s*$' <<< "{{.CARGO_PKGS}}" | xargs -r -n1 cargo install --locked

  install:
    desc: One-time per-user setup - git identity, shell, font, prezto/tpm submodules, ~/programs, fzf, npm/pipx/cargo apps
    preconditions:
      - sh: git --git-dir="$HOME/.dotfiles" rev-parse --verify HEAD >/dev/null 2>&1
        msg: "run `task utility:bare-install` first — $HOME is not the dotfiles work-tree yet"
    cmds:
      - task: keygen
        vars:
          GIT_EMAIL: "{{.GIT_EMAIL}}"
          SSH_KEY_PASSPHRASE: "{{.SSH_KEY_PASSPHRASE}}"
      - task: git-identity
        vars:
          GIT_NAME: "{{.GIT_NAME}}"
          GIT_EMAIL: "{{.GIT_EMAIL}}"
      - task: font
      - task: submodules
      - task: tpm
      - task: swift-map
      - task: wmutils
      - task: fzf
      - task: npm-globals
      - task: pipx-apps
      - task: rustup
      - task: cargo-apps

  update:
    desc: Update prezto/tpm submodules and plugins, fzf, and all npm/pipx/cargo apps
    preconditions:
      - sh: git --git-dir="$HOME/.dotfiles" rev-parse --verify HEAD >/dev/null 2>&1
        msg: "run `task utility:bare-install` first — $HOME is not the dotfiles work-tree yet"
    cmds:
      - task: submodules
      - task: tpm
      - task: fzf-update
      - task: npm-globals
      - task: pipx-apps
      - task: rustup-update
      - task: cargo-apps
